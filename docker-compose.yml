services:
  go:
    container_name: go-scraper
    image: cryptoviz/scraper:develop
    restart: always
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/app
    working_dir: /app